import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    container:{
        margin:10,
    },
    title:{
        color:'white',
        fontSize:22,
        fontWeight:'bold',
        margin:10
    }
});

export default style;